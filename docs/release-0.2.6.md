# 举个铁子 v0.2.6

构建号：20260922.4。

- 配置 GitHub `HectorGao/big3` 的源码同步流程，后续按完成修改、测试通过、审查、提交、推送执行。
- 增加 GitHub Actions：只读权限、固定 Action 版本、Node 24、测试和白名单构建、静态产物下载。不自动部署网站或修改 DNS。
- 增加通用 `build:static` 和 EdgeOne Pages 配置，提供 Cloudflare Pages 接入参数。
- 移除待公开源码中的固定 demo 密码，新环境使用随机私密凭证；原本机账号、历史和草稿不变。
- 新增暂存区与发布分支历史的路径/敏感模式检查，不推送本地调试快照。

本地共享/后端测试 193 项通过，静态构建与内容校验通过。GitHub 云端检查结果以该提交的 Actions 状态为准。静态产物不提供后台登录、同步和共享数据服务；微信开发者工具、真机及正式云托管仍为独立验收。

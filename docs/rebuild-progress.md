# 50 动作库与训练处方重建

## 已确认范围
- 保留原有 22 个 ID，新增 28 个动作；原创写实解剖分阶段图片。
- 无同动作可靠负荷时引导试重，禁止按三大项重量换算辅助动作。
- 日期/模式/安全校验共用逻辑，试重与实际工作组分离，保留旧数据。
- 仅本地 Git 提交，不推送、不提交微信审核。

## 当前进度
- [x] 检查代码与现有基线 bddb9cb。
- [x] 内置图像生成制作全部 50 套，147 个阶段；不使用旧简图补位。
- [x] 50 动作定义、计量规则、分阶段素材与逐项初步视觉检查。检查记录见 `media-sources/*.json`。
- [x] 共享处方、独立试重、器械身份与档位、v3 备份优先迁移。
- [x] 网页/微信页面接入，五个教学详情分包，原图不进入发布包。
- [x] 共享逻辑、微信页面控制器模拟、网页手机/桌面流程回归。
- [x] 分批本地 Git 提交：动作库与图册、校准与规划、两端联调与回归；不推送远程。

## 本轮验证（2026-09-19）
- `npm test`：117/117 通过。包括三项 × 六类训练日 × 两种目标、疲劳 1–5、疼痛、器械不足、部分完成、变更项目、延期、跳过、PB 保护、未来日期、重复保存、迁移与写入失败。
- 对应回归：硬拉强度日为传统硬拉、腿弯举、胸托划船、死虫式；无可靠辅助成绩为待试重；已有同动作负荷不退回固定 5 kg。
- 连续两次完整达标才增加一个实际器械档位；部分完成和重复来源不能解锁加重，失败或动作失控会降量。
- 浏览器 390 / 1280 px：类型切换改变真实负荷和次数、试重填充、开始后冻结、次数单位、未来开始拦截均通过。使用隔离测试存储，不修改用户档案。
- 全图册浏览器检查：两种视口各 50 套、147 个阶段，检查加载、像素非空、阶段说明、放大和横向溢出。截图人工检查补充自动检查，发现并修复阶段图两侧露出邻图的问题。
- 原图逐项初步视觉筛查覆盖握点、器械方向、支撑与动作阶段；卧推、前蹲、相扑硬拉、背伸、登阶、反向飞鸟等不合格初稿已重绘。该筛查由助手完成，不代表专业认证。
- 包体静态检查：主包约 0.34 MiB，五个分包各约 0.97–1.43 MiB，低于本项目每包 1.8 MiB 保守预算；正式微信构建结果仍待验证。

## 数据与运行
- 既有 22 个动作 ID 保留；旧记录不补造余力、单位确认或成功反馈，不自动乘二/折半。
- 原始迁移备份先于写入，试重与工作组原子保存；失败不清除草稿。待试重不能记为完成工作组。
- 浏览器预览：运行 `npm run preview` 后访问终端输出的本机地址；完整图册为 `/preview/figures.html`。
- 浏览器检查使用已安装 Playwright/Chrome。本机可设置 `NODE_PATH=/Users/hector/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules` 后运行两项浏览器检查脚本；这不是项目运行时依赖。

## 科学边界
ACSM 2009/2026 和自我调节综述支持目标导向及反馈调节原则，不验证本产品的固定组次、阈值或个人最优效果。生成图需逐项检查，不等于教练实地审定。

## 验证边界
微信开发者工具/真机尚未验证；后续如不可运行必须继续明确标注。

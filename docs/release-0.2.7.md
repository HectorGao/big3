# 举个铁子 v0.2.7

构建号：20260922.5。

修正静态站点访问账号接口失败时的提示：不再让线上用户打开本地服务器，明确在线账号服务不可用时登录、跨设备同步和留言板暂不可用，本机训练记录仍保留。

这次提示修正没有增加或部署账号后端，不代表登录故障已解决。现有 Node/SQLite 后端仍需持续运行环境与持久数据库。Cloudflare 默认站点在用户的大陆网络不可访问；国内入口正在接入 EdgeOne，其正式域名、备案和后端需分别验证。

验证：`npm run build:static` 通过源码检查、193 项测试、白名单构建与静态资源校验；浏览器点击静态产物的登录入口后显示新的准确提示。

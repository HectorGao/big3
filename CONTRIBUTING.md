# 举个铁子 · 开发与同步

[项目首页](README.md) · [文档导航](docs/README.md) · [安全说明](SECURITY.md)

## 修改之前

1. 阅读仓库 [AGENTS.md](AGENTS.md)，检查当前分支、未提交改动及相关调用方，保留其他人的工作。
2. 用 Node.js 24 和 `npm ci` 恢复锁定依赖。正常运行使用已提交的优化图片，不要求重新生成全部素材。
3. 训练算法在 `miniprogram/lib/` 共用，网页和微信分别接入；不要复制另一套处方逻辑。
4. 历史记录、进行中冻结目标、档案及备份必须兼容。不能通过换存储键、清库或重写旧记录来通过测试。

## 完成与检查

```sh
npm test
npm run check:repo
npm run build:static
git diff --check
```

`build:static` 本身已包含全套 Node 测试、源码排查和静态资源校验。相关代码未改变时可复用同一轮结果，不需要为了增加次数重复测试。

界面改动需运行对应浏览器检查，包含手机视口和实际输入 / 保存；素材检查要覆盖姿势与器械方向，不能只确认文件存在。浏览器脚本需要可用的 Playwright 与 Chromium，环境不具备时明确列为未验证。原生微信、真机和模型有效性分别报告。

修改后端用隔离临时数据库，不向实际 `runtime/big3.sqlite` 写入测试账号、模拟训练或恢复测试。文档中的接口、路径、命令和版本应与代码一致；历史报告保留当时语境，不伪装成当前验收。

## 提交与同步

仓库所有者已确认：每次需求完成并通过相关测试后提交、推送。不是自动上传每次保存，不运行后台文件监听，也不推送未完成工作。

1. 审查差异，使用具体文件路径暂存，不能使用 `git add .` 或 `git add -A`。
2. 暂存后执行 `npm run check:repo -- --staged --history`，再检查暂存差异。
3. 写明修改目的、测试与边界，创建提交。
4. 推送明确分支 `git push origin main:main`；远端有新提交时先获取和比较，不强制覆盖。
5. 核实 GitHub 分支提交及 Actions 结果；检查失败时说明失败，不宣称同步完成。

只推送已授权的 GitHub 仓库，不推送 `sites`、标签或本地恢复引用，不使用 `--all`、`--mirror`、强制推送或历史重写。Git 历史检查覆盖待发布 `HEAD` 及祖先，不代表其他本地检查点已审查。

有界面或行为变化的发布需更新 `miniprogram/lib/release.js`、`package.json` 和锁文件版本并补发布记录；纯文档更新不虚增应用版本。保留原数据键、动作 ID 和 API 兼容标识，不因品牌换名清空旧记录。

## 发布与资料保护

GitHub 检查生成可下载的静态包，不会自动开通后端、绑定域名或提交微信。需发布时只使用白名单 `dist/`，实际部署另行授权与验收。

不得提交用户数据、可复用密码、令牌、数据库、备份、研究样本或原始生成图片。遇到漏入历史的敏感内容，先停止推送并说明，不擅自重写历史。测试数据只用人工生成样本。

本仓库尚未声明项目级开源许可证；不要自行增加许可或承诺第三方素材权利。添加文献时写清适用人群、限制和哪些阈值仅是产品规则。

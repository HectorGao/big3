# 三项训练

原生微信小程序源码 + 使用同一训练逻辑的浏览器预览。

## 本地版本管理与审查

当前项目目录为 `/Users/hector/work_space/Codex/apps/muscle`，使用独立本地 Git 仓库管理。迁移后的审查结果与已知问题见 [2026-09-17 审查报告](docs/review-2026-09-17.md)。初始基线包含待修复问题，不代表功能全部验收或已发布。

日常使用 `git status` 查看状态，`git diff` 审查改动，再按明确文件路径执行 `git add` 与 `git commit`。`node_modules`、本机私有配置、凭证和训练导出不纳入版本管理；依赖通过 `npm ci` 恢复。浏览器/微信存储里的实际训练数据不在 Git 中，需要另行导出备份。

本轮重建内容采用分批本地提交，不配置远程仓库、不上传源码或微信审核。最新验收记录见 [重建进度](docs/rebuild-progress.md)。

## 查看预览

运行 `npm ci` 后，直接用浏览器打开 `preview/index.html`，或运行 `npm run preview`，访问终端打印的地址（默认 http://127.0.0.1:4173）。预览服务仅监听本机；退出服务或重启电脑后需重新启动。直接打开 HTML 不需要服务，但浏览器必须允许此文件使用本地存储。

`?qa=名称` 使用独立测试存储，不向用户档案注入演示数据。网页预览和微信小程序的数据相互独立。

## 微信导入

在微信开发者工具中导入本项目根目录，`project.config.json` 已指向 `miniprogram/`。目前 `appid` 是游客占位 `touristappid`，正式预览、上传和审核必须替换为账号真实 AppID；不能凭游客配置提交。建议使用当前稳定版开发者工具与基础库 3.0.0 及以上。

小程序自身无 npm 运行时依赖，无服务器域名和云服务依赖。`lucide` 仅用于浏览器预览图标。

## 已实现

- 概览、今日训练、肌肉图谱、训练记录、我的档案五个页面。
- 50 个动作、正背面点击与器械筛选、动作要点及替换；上背与腰背独立分区。
- 深蹲、卧推、硬拉 PB（重量、次数、日期），年龄、体重、经验、目标和训练日设置。
- 容量、强度、恢复、技术与休息规则；无 PB 可用；主项按总重量增量向下取整。
- 逐组实际记录、计时、部分完成、同日草稿续接、历史明细与单条删除。
- 本地 JSON 备份与恢复，坏数据不自动覆盖，写入失败提示。
- 实时 Epley/Brzycki 估算 PB 与保守训练基准；可靠同动作记录支持预填，没有可靠负荷时显示“待试重”，不伪造公斤数。
- 有同动作容量依据时，加重自动降低建议次数；逐组实际 RIR 驱动后续校准。
- 50 套原创写实教学图、147 个阶段，逐阶段查看及放大；完整图册在 `preview/figures.html`。初步视觉筛查不等于专业教练审定。
- 正背面有独立全身轮廓与外观标志；卧推使用横向杠铃的斜上方视角。
- 疲劳 1–5 档有描述与自动调整；休息时能查看后续计划，训练进行中可浏览其他主项且保留草稿。

## 训练逻辑边界

本项目提供一般健身模板，并非经验证的个体处方。PB 的 2–10 次同时使用 Epley 和 Brzycki 公式，单次直接返回实测重量。使用两公式较低值乘以保守系数作为训练基准，再从目标次数与余力反算工作重量。增肌与力量目标有不同次数和组数配置。完整公式、文献来源、初始试重及产品默认值的区别见 `docs/scientific-basis.md`，界面可点击“计算依据”。

新手、65 岁及以上、长期中断或无近期 PB 使用技术适应模板。疲劳较高时降量；新手疲劳恢复不超过技术日负荷。疼痛阻止计划生成。近期深蹲/硬拉互相检查下肢恢复间隔。未完成主项次数不推进周期，最近一次未完成训练的高 RPE 也参与判断。

跨日草稿不允许继续打组；归档使用用户确认的已发生日期，只保存实际完成组。年龄、体重不直接修改实际 PB，也不推算辅助重量。未知负荷需实际试重，之后仅用同动作、同器械、相同计量的记录与余力调整。试重独立保存，不计成绩与工作组，但参与恢复判断。具体研究支持原则、产品规则及个体校准参数见 [处方依据](docs/prescription-evidence.md)。

广泛训练原则参考：https://acsm.org/resistance-training-guidelines-update-2026/

## 验证与发布状态

`npm test` 执行 Node 内置测试，包括共享逻辑、持久化和模拟微信页面控制器。浏览器交互和屏幕检查见 `docs/verification.md`。模拟测试不能代替微信编译或真机运行。

尚未在微信开发者工具编译或进行真机测试，尚未提交微信、也未上线。当前机器未发现微信开发者工具，账号 AppID 和提交权限未提供。发布还需账号主体、服务类目、备案（按后台要求）、隐私声明、最终素材信息及审核通过。发布流程：https://developers.weixin.qq.com/miniprogram/dev/framework/quickstart/release.html

## 素材

人体图由 `lib/anatomy.js` 中原创 Canvas 几何生成，为简化肌群示意。器械与动作图片由内置图像工具原创生成，没有复制 MuscleWiki 图像、视频或源码。动作提示为本项目编写，未包含动作教学视频。

原图保留在发布排除目录 `art-originals/`，不进入 Git 或小程序；来源和逐图检查意见在 `docs/media-sources/`。优化图片和详情页放入五个动作分包，网页复用同一素材清单。已提交的优化素材可直接运行，无需重新生成；本机原图可通过 `npm run build:media` 重新打包。`npm run check:release-media` 检查 50 套覆盖率与包体预算，不等于微信编译包体验证。

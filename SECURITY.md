# 举个铁子 · 安全与隐私

[项目首页](README.md) · [后端边界](server/README.md) · [生产与备份](docs/production-accounts-backup.md)

## 公开仓库不包含什么

本仓库只发布应用代码、优化教学素材和说明文档。不应包含管理员或测试密码、令牌、数据库、用户档案、训练备份、研究快照及派生模型。`.gitignore` 和 `check:repo` 提供防误提交检查，但不能保证识别所有敏感内容。

测试账号在本地需要时创建，新密码随机生成并仅写入私密文件；不存在公开通用登录口令。生产环境禁止模拟账号数据库与调试接口。管理员通过服务器终端初始化，普通注册只创建成员账号。

## 数据边界

- 注册资料和训练记录保存在所运行后端的数据库；管理员具有查看与导出权限，相关操作有审计。
- 未隐藏的训练回声通过公开展示接口返回，不适合发布私密训练信息或联系方式。
- 管理员档案下载是未加密的敏感 JSON，不含密码但也不是完整数据库恢复包。GitHub 不承担训练记录备份。
- 研究导出必须有独立授权；去标识不保证匿名，撤回后下载副本及派生结果仍需人工治理。
- 退出账号不保证移除本机缓存。在共享设备上应使用独立浏览器会话，避免他人访问遗留档案。

## 运行保护与限制

当前实现包含带盐密码哈希、HttpOnly / SameSite 会话 Cookie、来源检查、数据库参数化查询、服务端权限与并发版本校验。正式 HTTPS 下 Cookie 使用 Secure 标记。

这些措施不等于独立安全审计或生产认证。密码找回、账号注销、自动灾难恢复、多实例限流、监控告警和授权撤回自动处理尚未完善。对外部署之前必须验证 HTTPS 代理、管理员初始化、持久化与恢复，并明确用户隐私告知。

不要将源码根目录、SQLite 文件或环境配置设为静态站点内容。按 [静态部署文档](docs/github-static-hosting.md) 使用 `dist/`；它不能单独提供账号后端。

## 报告问题

普通功能问题可提交不含隐私的最小复现。涉及密码、越权或个人资料时，不要公开发布利用细节或真实数据；先通过与维护者已有的私密联系渠道沟通。如果尚无私密渠道，只公开请求建立联系，不贴凭证、备份或漏洞细节。

本项目目前没有声明专用安全邮箱、保密工单平台或响应时限，不应假设普通 GitHub Issue 是私密的。意外暴露凭证时应先停止传播并撤销或轮换相关凭证，再与维护者协商清理历史和副本；仅删除当前文件不能消除已公开的旧提交。
